"use client";
import { useNavigate } from "react-router-dom";

const Dashboard = () => {
    const router = useRouter();
    
    return (
        <div>
            Hello World!
        </div>
    );
};

export default Dashboard;

